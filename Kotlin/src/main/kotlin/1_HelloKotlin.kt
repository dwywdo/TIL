/**
 * Below is called package-level function because it doesn't belong to any other class
 * In Kotlin, we do not use semicolon(;) at the end of each statements.
 *  We use semicolon only for writing more than 1 statement in one line.
 */
fun main(args: Array<String>): Unit { // Same as String[] args in JAVA
    println("Hello, Kotlin!")
}
