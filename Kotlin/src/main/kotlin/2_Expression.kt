/**
 * Value itself such as 53, 62, 126 is called 'Literal'
 */
fun main(args: Array<String>): Unit {
    /**
     * JAVA doesn't allow expression like below.
     * In detail, it doesn't allow to write only expression
     */
    53 + 62 - 126
}
